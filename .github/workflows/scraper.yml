name: Auto Scraper

on:
  workflow_dispatch:  # 👈 Add this to enable manual runs
  schedule:
    - cron: "0 */6 * * *"  # Runs every 6 hours
  push:
    branches:
      - main

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: List all files (Debugging)
        run: ls -R

      - name: Run scraper
        run: node scrapper.js  # Update if filename is incorrect

      - name: Commit and push changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add universe_data.json
          git commit -m "Auto-update universe_data.json" || exit 0
          git push

